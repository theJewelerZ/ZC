import { readFileSync } from "node:fs";import { join } from "node:path";import { describe,expect,it } from "vitest";
const sql=readFileSync(join(process.cwd(),"supabase/migrations/20260805000200_create_project_lifecycle.sql"),"utf8");
describe("project lifecycle migration",()=>{it("keeps operational, publication, and public build states separate",()=>{expect(sql).toContain("operational_status");expect(sql).toContain("publication_status");expect(sql).toContain("public_build_status")});it("forces RLS and denies direct browser access",()=>{for(const table of ["projects","project_updates","project_photos"]){expect(sql).toContain(`alter table public.${table} force row level security`);expect(sql).toContain(`revoke all on table public.${table} from anon, authenticated`)}});it("separates private originals from public approved copies",()=>{expect(sql).toContain("project-media-private");expect(sql).toContain("project-media-public");expect(sql).toContain("caption is not null");expect(sql).toContain("alt_text is not null");expect(sql).toContain("approval_status = 'approved'")})});
