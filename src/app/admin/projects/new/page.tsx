import type { Metadata } from "next";import Link from "next/link";import { createProjectAction } from "@/app/admin/projects/actions";import { AdminNav } from "@/components/admin/admin-nav";import { requireAdmin } from "@/lib/admin/auth";
export const metadata:Metadata={title:"Create project",robots:{index:false,follow:false,nocache:true}};export default async function NewProject(){await requireAdmin();return <main className="admin-shell" id="main-content"><AdminNav/><Link className="admin-back" href="/admin/projects">← Projects</Link><section className="admin-card"><p className="eyebrow">Private by default</p><h1>Create project</h1><form action={createProjectAction} className="admin-update-form"><label htmlFor="internalName">Internal project name</label><input id="internalName" maxLength={160} name="internalName" required/><label htmlFor="publicTitle">Public Build title</label><input id="publicTitle" maxLength={160} name="publicTitle" required/><label htmlFor="slug">Public URL slug</label><input id="slug" maxLength={120} name="slug" pattern="[a-z0-9]+(?:-[a-z0-9]+)*"/><p className="field-help">Leave blank to generate from the internal name. Nothing will be published.</p><button className="button button-primary">Create private project</button></form></section></main>}
