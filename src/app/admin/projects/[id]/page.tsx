/* eslint-disable @next/next/no-img-element */
import type { Metadata } from "next";import Link from "next/link";import { notFound } from "next/navigation";import { AdminNav } from "@/components/admin/admin-nav";import { ProjectPhotoUploader } from "@/components/admin/project-photo-uploader";import { getSupabasePublicConfig } from "@/lib/supabase/config";import { createProjectUpdateAction,setProjectPublicationAction,setUpdatePublicationAction,updatePhotoPublicationAction,updateProjectAction } from "@/app/admin/projects/actions";import { requireAdmin } from "@/lib/admin/auth";import { createSupabaseAdminClient } from "@/lib/supabase/admin";import { isUuid,label,operationalStatuses,projectStages,publicBuildStatuses,PROJECT_PRIVATE_BUCKET } from "@/lib/projects/schema";
export const dynamic="force-dynamic";export const metadata:Metadata={title:"Project workspace",robots:{index:false,follow:false,nocache:true}};
export default async function ProjectDetail({params}:{params:Promise<{id:string}>}){await requireAdmin();const{id}=await params;if(!isUuid(id))notFound();const client=createSupabaseAdminClient();const publicConfig=getSupabasePublicConfig();const[{data:project},{data:updates},{data:photos}]=await Promise.all([client.from("projects").select("*").eq("id",id).maybeSingle(),client.from("project_updates").select("*").eq("project_id",id).order("occurred_on",{ascending:false}),client.from("project_photos").select("*").eq("project_id",id).eq("upload_state","complete").order("created_at")]);if(!project)notFound();const signed=await Promise.all((photos||[]).map(async(photo)=>({...photo,url:(await client.storage.from(PROJECT_PRIVATE_BUCKET).createSignedUrl(photo.private_storage_path,300)).data?.signedUrl||null})));return <main className="admin-shell" id="main-content"><AdminNav/><Link className="admin-back" href="/admin/projects">â† Projects</Link><div className="admin-heading"><div><p className="eyebrow">{label(project.publication_status)}</p><h1>{project.internal_name}</h1><p>Private workspace. Public copy is marked separately.</p></div><div className="admin-security-actions">{project.publication_status==="published"?<Link className="button button-outline" href={`/projects/${project.slug}`} target="_blank">View public Build</Link>:null}<form action={setProjectPublicationAction}><input name="id" type="hidden" value={id}/><input name="intent" type="hidden" value={project.publication_status==="published"?"unpublish":"publish"}/><button className="button button-primary">{project.publication_status==="published"?"Unpublish":"Publish Build"}</button></form></div></div><section className="admin-card"><h2>Project information</h2><form action={updateProjectAction} className="admin-project-form"><input name="id" type="hidden" value={id}/><label>Internal name<input defaultValue={project.internal_name} name="internalName" required/></label><label>Public title<input defaultValue={project.public_title} name="publicTitle" required/></label><label>Slug<input defaultValue={project.slug} name="slug" required/></label><label>Public location<input defaultValue={project.public_location||""} name="publicLocation"/></label><label className="admin-wide">Public story summary<textarea defaultValue={project.public_summary||""} name="publicSummary" rows={4}/></label><label>Operational status<select defaultValue={project.operational_status} name="operationalStatus">{operationalStatuses.map(v=><option key={v} value={v}>{label(v)}</option>)}</select></label><label>Project stage<select defaultValue={project.project_stage} name="projectStage">{projectStages.map(v=><option key={v} value={v}>{label(v)}</option>)}</select></label><label>Public Build status<select defaultValue={project.public_build_status} name="publicBuildStatus">{publicBuildStatuses.map(v=><option key={v} value={v}>{label(v)}</option>)}</select></label><label>Start date<input defaultValue={project.started_on||""} name="startedOn" type="date"/></label><label>Completion date<input defaultValue={project.completed_on||""} name="completedOn" type="date"/></label><label>Private address<input defaultValue={project.private_address||""} name="privateAddress"/></label><label className="admin-wide">Internal scope<textarea defaultValue={project.internal_scope||""} name="internalScope" rows={5}/></label><label className="admin-wide">Private notes<textarea defaultValue={project.internal_notes||""} name="internalNotes" rows={5}/></label><label className="admin-check"><input defaultChecked={project.featured_on_homepage} name="featured" type="checkbox"/> Feature on homepage when published</label><button className="button button-primary">Save project</button></form></section><section className="admin-card"><h2>Progress updates</h2><form action={createProjectUpdateAction} className="admin-update-form"><input name="projectId" type="hidden" value={id}/><label>Update title<input name="title" required/></label><label>Story<textarea minLength={10} name="body" required rows={5}/></label><label>Stage<select defaultValue={project.project_stage} name="projectStage">{projectStages.map(v=><option key={v} value={v}>{label(v)}</option>)}</select></label><label>Date<input defaultValue={new Date().toISOString().slice(0,10)} name="occurredOn" type="date"/></label><button className="button button-primary">Save private update</button></form>{updates?.map(update=><article className="admin-update-row" key={update.id}><div><strong>{update.title}</strong><p>{update.body}</p><small>{update.occurred_on} Â· {label(update.publication_status)}</small></div><form action={setUpdatePublicationAction}><input name="id" type="hidden" value={update.id}/><input name="projectId" type="hidden" value={id}/><input name="intent" type="hidden" value={update.publication_status==="published"?"unpublish":"publish"}/><button className="button button-outline">{update.publication_status==="published"?"Make private":"Publish update"}</button></form></article>)}</section><section className="admin-card"><h2>Project photography</h2><p>Uploads and originals remain private. Approved public copies require a caption and alt text.</p>{publicConfig?<ProjectPhotoUploader projectId={id} publishableKey={publicConfig.publishableKey} url={publicConfig.url}/>:<p className="notification-warning">Photo storage is not configured.</p>}{signed.map(photo=><form action={updatePhotoPublicationAction} className="admin-photo-review" key={photo.id}>{photo.url?<img alt="Private project preview" src={photo.url}/>:null}<input name="id" type="hidden" value={photo.id}/><input name="projectId" type="hidden" value={id}/><label>Caption<input defaultValue={photo.caption||""} name="caption"/></label><label>Alt text<input defaultValue={photo.alt_text||""} name="altText"/></label><input name="intent" type="hidden" value={photo.visibility==="public"?"unpublish":"publish"}/><button className="button button-outline">{photo.visibility==="public"?"Make private":"Approve and publish"}</button></form>)}</section></main>}
