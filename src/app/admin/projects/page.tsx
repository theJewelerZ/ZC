import type { Metadata } from "next";import Link from "next/link";import { AdminNav } from "@/components/admin/admin-nav";import { requireAdmin } from "@/lib/admin/auth";import { createSupabaseAdminClient } from "@/lib/supabase/admin";import { label } from "@/lib/projects/schema";
export const dynamic="force-dynamic";export const metadata:Metadata={title:"Projects",robots:{index:false,follow:false,nocache:true}};
export default async function ProjectsAdmin(){await requireAdmin();const{data,error}=await createSupabaseAdminClient().from("projects").select("*").order("updated_at",{ascending:false});if(error)throw new Error("Projects could not be loaded.");return <main className="admin-shell" id="main-content"><AdminNav/><div className="admin-heading"><div><p className="eyebrow">Founder dashboard</p><h1>Projects</h1><p>Private operational records become public Builds only after founder approval.</p></div><Link className="button button-primary" href="/admin/projects/new">Create project</Link></div>{data?.length?<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Project</th><th>Stage</th><th>Operations</th><th>Publication</th></tr></thead><tbody>{data.map((project)=><tr key={project.id}><td><Link href={`/admin/projects/${project.id}`}><strong>{project.internal_name}</strong></Link><span>/{project.slug}</span></td><td>{label(project.project_stage)}</td><td>{label(project.operational_status)}</td><td><span className="status-badge">{label(project.publication_status)}</span></td></tr>)}</tbody></table></div>:<div className="admin-empty"><h2>No projects yet.</h2><p>Create Mason Simulator Environment or Social Golfer Simulator Environment when you are ready. New records are private.</p></div>}</main>}
