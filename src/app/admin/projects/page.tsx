import type { Metadata } from "next";import Link from "next/link";import { AdminNav } from "@/components/admin/admin-nav";import { requireAdmin } from "@/lib/admin/auth";import { createSupabaseAdminClient } from "@/lib/supabase/admin";import { label } from "@/lib/projects/schema";
export const dynamic="force-dynamic";export const metadata:Metadata={title:"Builds",robots:{index:false,follow:false,nocache:true}};
export default async function ProjectsAdmin(){await requireAdmin();const{data,error}=await createSupabaseAdminClient().from("projects").select("*").order("updated_at",{ascending:false});if(error)throw new Error("Builds could not be loaded.");return <main className="admin-shell" id="main-content"><AdminNav/><div className="admin-heading"><div><p className="eyebrow">Founder operations</p><h1>Builds</h1><p>Private operational records become public Build stories only after founder approval.</p></div><Link className="button button-primary" href="/admin/projects/new">Create Build</Link></div>{data?.length?<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Project</th><th>Stage</th><th>Operations</th><th>Publication</th></tr></thead><tbody>{data.map((project)=><tr key={project.id}><td data-label="Build"><Link href={`/admin/projects/${project.id}`}><strong>{project.internal_name}</strong></Link><span>/{project.slug}</span></td><td data-label="Stage">{label(project.project_stage)}</td><td data-label="Operations">{label(project.operational_status)}</td><td data-label="Publication"><span className="status-badge">{label(project.publication_status)}</span></td></tr>)}</tbody></table></div>:<div className="admin-empty"><h2>No projects yet.</h2><p>Create Mason Simulator Environment or Social Golfer Simulator Environment when you are ready. New records are private.</p></div>}</main>}
