/* eslint-disable @next/next/no-img-element */
import type { Metadata } from "next";import { notFound } from "next/navigation";import { businessConfig } from "@/config/business";import { getPublishedBuild } from "@/lib/projects/repository";import { label } from "@/lib/projects/schema";
export const revalidate=300;
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const result=await getPublishedBuild(slug);if(!result)return{};return{title:result.project.public_title,description:result.project.public_summary,alternates:{canonical:`/projects/${slug}`},openGraph:{title:result.project.public_title,description:result.project.public_summary||undefined,url:new URL(`/projects/${slug}`,businessConfig.canonicalUrl)}}}
export default async function BuildDetail({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const result=await getPublishedBuild(slug);if(!result)notFound();const{project,updates,photos}=result;return <main id="main-content"><header className="build-detail-hero"><div className="site-container"><p className="eyebrow">Inside the Build · {label(project.public_build_status)}</p><h1>{project.public_title}</h1>{project.public_location?<p className="build-location">{project.public_location}</p>:null}<p>{project.public_summary}</p></div></header>{photos.length?<section className="section"><div className="site-container build-gallery">{photos.map(photo=><figure key={photo.id}><img alt={photo.alt_text||""} src={photo.url}/><figcaption>{photo.caption}</figcaption></figure>)}</div></section>:null}{updates.length?<section className="section build-updates"><div className="site-container"><p className="eyebrow">Progress story</p><h2>Updates from the Build</h2><div className="build-timeline">{updates.map(update=><article key={update.id}><time dateTime={update.occurred_on}>{new Date(`${update.occurred_on}T12:00:00`).toLocaleDateString("en-US",{dateStyle:"long"})}</time><h3>{update.title}</h3><p>{update.body}</p></article>)}</div></div></section>:null}</main>}
